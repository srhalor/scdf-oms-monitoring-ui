# =============================================================================
# Docker Ignore File
# Excludes files/directories from Docker build context for faster builds
#
# Strategy: Exclude everything, then whitelist only what's needed
# Prerequisites: Run `pnpm install && pnpm build` before docker build
# =============================================================================

# Exclude everything by default
*

# Whitelist: Only include what's needed for production image
!public
!.next/standalone
!.next/static
!certificates
