registry=https://registry.npmjs.org/

# Use hoisted node_modules structure (no symlinks) for Docker compatibility
# This ensures standalone builds work when copying from Windows to Linux containers
node-linker=hoisted
